{
  "manifest_version": 3,
  "name": "GitHub PR Tab Group Sync",
  "version": "1.3.1",
  "description": "Keeps a Chrome tab group per repo mirroring its open pull requests. New PRs are prepended to the top of the group; closed/merged PRs are removed.",
  "permissions": ["tabs", "tabGroups", "storage", "alarms"],
  "host_permissions": ["https://api.github.com/*"],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  },
  "options_page": "options.html",
  "action": {
    "default_title": "GitHub PR Tab Group Sync (click to open settings)"
  }
}
